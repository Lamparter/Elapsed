<Page
	x:Class="Riverside.Elapsed.App.Presentation.RecordingPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:utu="using:Uno.Toolkit.UI"
	Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
	<Grid utu:SafeArea.Insets="VisibleBounds" Margin="24">
		<Border
			MaxWidth="560"
			Padding="28"
			HorizontalAlignment="Center"
			VerticalAlignment="Center"
			Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
			CornerRadius="10">
			<StackPanel Spacing="16">
				<TextBlock FontSize="30" FontWeight="SemiBold" Text="Recording" />
				<TextBlock Opacity="0.75" Text="{Binding Status}" />
				<StackPanel Orientation="Horizontal" Spacing="10">
					<Button Command="{Binding PauseResumeCommand}" Content="{Binding PauseResumeText}" />
					<Button Command="{Binding StopCommand}" Content="Stop" />
					<Button Command="{Binding BackCommand}" Content="Back" />
				</StackPanel>
			</StackPanel>
		</Border>
	</Grid>
</Page>
