<Page
	x:Class="Riverside.Elapsed.App.Presentation.LoginPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:utu="using:Uno.Toolkit.UI"
	Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
	NavigationCacheMode="Required">
	<Grid utu:SafeArea.Insets="VisibleBounds" Margin="24">
		<Border
			MaxWidth="540"
			Padding="28"
			HorizontalAlignment="Center"
			VerticalAlignment="Center"
			Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
			CornerRadius="12">
			<StackPanel Spacing="12">
				<Image
					Width="120"
					Height="120"
					HorizontalAlignment="Center"
					Source="ms-appx:///Assets/Illustrations/orpheus-time.png" />
				<TextBlock HorizontalAlignment="Center" FontSize="28" FontWeight="SemiBold" Text="{Binding Title}" />
				<TextBlock HorizontalAlignment="Center" Opacity="0.7" Text="{Binding Description}" />
				<Button
					MinWidth="220"
					HorizontalAlignment="Center"
					Command="{Binding LoginCommand}"
					Content="Sign in with Lapse" />
				<TextBlock
					HorizontalAlignment="Center"
					Foreground="{ThemeResource SystemFillColorCriticalBrush}"
					Text="{Binding Message}"
					TextWrapping="Wrap" />
			</StackPanel>
		</Border>
	</Grid>
</Page>
