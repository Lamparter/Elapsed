<Page
	x:Class="Riverside.Elapsed.App.Presentation.VideoPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:controls="using:Riverside.Elapsed.App.Presentation.Controls"
	xmlns:utu="using:Uno.Toolkit.UI"
	Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
	<Grid utu:SafeArea.Insets="VisibleBounds">
		<Grid.ColumnDefinitions>
			<ColumnDefinition />
			<ColumnDefinition Width="380" />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition />
		</Grid.RowDefinitions>

		<StackPanel Grid.ColumnSpan="2" Orientation="Horizontal" Margin="24,16,24,12" Spacing="8">
			<Button Command="{Binding BackCommand}" Content="Back" />
			<TextBlock VerticalAlignment="Center" FontSize="24" FontWeight="SemiBold" Text="Video" />
		</StackPanel>

		<controls:TimelapsePlayerControl Grid.Row="1" Margin="24,0,12,24" />

		<Border
			Grid.Row="1"
			Grid.Column="1"
			Margin="12,0,24,24"
			Padding="12"
			Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
			CornerRadius="8">
			<ScrollViewer HorizontalScrollMode="Disabled">
				<ItemsRepeater ItemsSource="{Binding RelatedTimelapses}">
					<ItemsRepeater.Layout>
						<StackLayout Orientation="Vertical" Spacing="10" />
					</ItemsRepeater.Layout>
					<ItemsRepeater.ItemTemplate>
						<DataTemplate>
							<StackPanel Orientation="Horizontal" Spacing="8">
								<Image Width="128" Height="72" Stretch="UniformToFill" Source="{Binding ThumbnailUrl}" />
								<StackPanel Width="200">
									<TextBlock FontWeight="SemiBold" Text="{Binding Title}" TextWrapping="WrapWholeWords" />
									<TextBlock Opacity="0.7" Text="{Binding OwnerText}" TextWrapping="WrapWholeWords" />
									<TextBlock Opacity="0.7" Text="{Binding MetaText}" />
								</StackPanel>
							</StackPanel>
						</DataTemplate>
					</ItemsRepeater.ItemTemplate>
				</ItemsRepeater>
			</ScrollViewer>
		</Border>
	</Grid>
</Page>
